---
description: Open Claude desktop app. Responds to "open claude", "open cc".
---

# Open Claude

Open the Claude desktop app.

Run the following command:

```bash
open -a Claude
```

After running, confirm to the user: "Opened Claude."

If the command fails, tell the user that Claude could not be found and suggest they verify it is installed.
