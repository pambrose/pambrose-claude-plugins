---
description: Open Ghostty terminal. Responds to "open ghostty".
---

# Open Ghostty

Open Ghostty terminal.

Run the following command:

```bash
open -a Ghostty
```

After running, confirm to the user: "Opened Ghostty."

If the command fails, tell the user that Ghostty could not be found and suggest they verify it is installed.
