---
description: Open macOS Terminal. Responds to "open terminal".
---

# Open Terminal

Open macOS Terminal.

Run the following command:

```bash
open -a Terminal
```

After running, confirm to the user: "Opened Terminal."

If the command fails, tell the user that Terminal could not be found.
