---
description: Open Warp terminal. Responds to "open warp".
---

# Open Warp

Open Warp terminal.

Run the following command:

```bash
open -a Warp
```

After running, confirm to the user: "Opened Warp."

If the command fails, tell the user that Warp could not be found and suggest they verify it is installed.
