---
description: Open macOS Finder to the current directory. Responds to "open finder".
---

# Open Finder

Open macOS Finder to the current working directory.

Run the following command:

```bash
open .
```

After running, confirm to the user: "Opened Finder in [current directory]."
